class Solution {
public:
    int diagonalSum(vector<vector<int>>& mat) {
        int sum=0;
        int n = mat.size();
        int i=0, j=0;
        while(i<mat[0].size() && (j < n)){
            int pos = j;
            ans += mat[i][pos];

            int pos2
        }
        return sum;
    }
};